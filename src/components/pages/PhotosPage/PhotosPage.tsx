import React from 'react';
import './photosPage.scss';

const PhotosPage: React.FC = () => {

    return (
        <section className="photos-page">
            <div className="photos-page__wrapper">
            </div>
        </section>
    );
};

export default PhotosPage;